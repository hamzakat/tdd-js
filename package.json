{
  "name": "test-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test": "jest --watchAll --verbose"
  },
  "devDependencies": {
    "vite": "^2.6.0"
  },
  "dependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.2.4"
  }
}
